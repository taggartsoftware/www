{
    "name": "www",
    "private": true,
    "version": "0.0.1",
    "scripts": {
        "build": "tsc --build",
        "start": "http-server docs -a localhost -p 8000 -c-1 -o /"
    },
    "devDependencies": {
        "http-server": "0.12.3",
        "typescript": "4.3.5"
    }
}